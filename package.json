{
  "name": "karuta-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
